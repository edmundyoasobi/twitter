class Profile{
    constructor(id, userName, userId, profileImagePath){
        this.id = id
        this.userName = userName
        this.userId = userId
        this.profileImagePath = profileImagePath
    }
}

export default Profile;